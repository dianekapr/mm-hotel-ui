export type VillaType = {
    id: number,
    name: string,
    description: string,
    imageUrl: string[]
}

export type ExperienceType = {
    id: number,
    name: string,
    description: string,
    price: string,
    imageUrl: string[]
}

export type PackageType = {
    id: number,
    name: string,
    duration: string,
    description: string,
    imageUrl: string[]
}

